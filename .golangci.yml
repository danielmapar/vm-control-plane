version: "2"

linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
    - copyloopvar
  exclusions:
    generated: lax
    paths:
      - proto

formatters:
  enable:
    - gofmt
    - goimports
